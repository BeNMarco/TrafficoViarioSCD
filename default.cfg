configuration default is
--pragma Starter(none);
pragma Starter (Ada);
pragma Name_Server (Embedded);


centralized_server : Partition:= (the_name_server,global_data,configuration_synchronized_package,configuration_cache_abitanti,configuration_posizione_abitanti);
procedure configuration_server;-- is in centralized_server;

quartiere1: Partition:=();
procedure main is in quartiere1;


--for quartiere1'Main use main;

for centralized_server'Main use configuration_server;

--for Partition'Directory use "bin";
for centralized_server'Termination use Global_Termination;
for quartiere1'Termination use Global_Termination;

end default;
